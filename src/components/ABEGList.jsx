
function ABEGList({list, title, color}) {
  return (
    <div className=" h-72 bg-white shadow-lg">
        <h1 className={`text-center py-2 ${color} font-bold uppercase`}>{title}</h1>
    </div>
  )
}

export default ABEGList
